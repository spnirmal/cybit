our deck needs a user interface for which i chose a TUI (terminal user interface) to match the aesthetic specifically over a GUI.
which eventually led me to ncurses library, this supports c,c++,rust etc. but our launcher will use c++ to make use of classes and objects just to make the code much modular and reusable.
